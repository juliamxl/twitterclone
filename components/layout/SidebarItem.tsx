import { IconType } from "react-icons/lib";

interface SidebarItemProps{
    label: string;
    href?: string;
    icon: IconType
}

const SidebarItem = () => {
    return(
        <div>

        </div>
    );
}

export default SidebarItem